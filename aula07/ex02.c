#include <stdio.h>

//Criar um algoritmo para calcular o perimetro e a area de um retangulo utilizando structs e funções
struct retangulo {
    float l1;
    float l2;
};

struct retangulo Lados (float lado1, float lado2)
{
    struct retangulo a;
}

int main ()
{

    return 0;
}